function Joiner( item, config ){
    this._select = [];
    this.name = item['ref'];
    this._conditions = { [item['foreignKey']]: [item['primaryKey']] };
    this._sort = config._sort;
    this._limit = config._limit;
    this._skip = config._skip;
};

module.exports = Joiner;